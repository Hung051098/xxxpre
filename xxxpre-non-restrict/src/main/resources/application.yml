google:
  oauth:
    client-id: ${GG_CLIENT_ID}
    client-secret: ${GG_CLIENT_SECRET}
    refresh-token: ${GG_REFRESH_TOKEN}
  api:
    key: AIzaSyCB2HisghU2vaub9n7onfq39dTPGQuSn04
  folder:
    id: 1VRJU5bG0BkSUsWkNTOK8-HRe3wgNY_-G
  service:
    account:
      email: xxxpre-drive-streamer@xxxpre.iam.gserviceaccount.com
      json: classpath:xxxpre.json
  ssl:
    disable: true  # Set to true for quick test, false for production
  truststore:
    file: classpath:truststore.jks
    password: changeit

spring:
  application:
    name: xxxpre
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/movie?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:mysql}
    password: ${DB_PASSWORD:your_password}
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      minimum-idle: 10
      maximum-pool-size: 200
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: 3GB
      max-request-size: 3GB

# === PROXY SETTINGS ===
# Replace with your actual proxy host (e.g., 10.x.x.x or proxy.company.com)
http:
  proxy:
    host: ${PROXY_HOST}
    port: ${PROXY_PORT}